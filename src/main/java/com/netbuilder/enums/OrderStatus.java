package com.netbuilder.enums;

/**
 * 
 * @author llew
 *
 */

public enum OrderStatus {
	placed, processing, awaitingDispatch, dispatched, delivered, cancelled, returned, refunded, wishlist, basket
}